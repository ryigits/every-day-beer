<form method="POST">
<div class="form">
      <div class="subtitle">Please Login</div>
      <div class="input-container">
        <input name="email" class="input" type="email" placeholder="Email" />
      </div>
      <div class="input-container">
        <input name="password" class="input" type="password" placeholder="Password" />
      </div>
      <button type="text" class="submit">Login</button>
    </div>
    </form>
    <div id="countDown">
{{reloadPage}}
</div>
<div id="registerLogin">Hey!Not member yet?<a style="text-decoration: none;" href="/register">sign up</a></div>
{{#if logAgain}}
  <h3 id="error">Please log in again</h3>
  {{/if}}
  {{#if showBlankError}}
  <h3 id="error">Err:dont leave blank</h3>
  {{/if}}

  {{#if doesNotMatch}}
    <h3 id="error">Err:password or email does not match</h3>
{{/if}}

