<h1 class="pageTitle">Edit Profile</h1>
{{#profile}}
<form method="POST">
<div class="input-container">
  <label for="first_name">First Name</label>
  <input name="first_name" class="input" type="text" minlength="3" value="{{first_name}}"/>
  <label for="last_name">Last Name</label>
  <input name="last_name" class="input" type="text" minlength="3" value="{{last_name}}"/>
  <label for="age">Age</label>
  <input name="age" id="emptyInput" class="input" type="number" min="18" max="99" minlength="1" value="{{age}}" />
  <label for="city">City</label>
  <input name="city" id="emptyInput" class="input" type="text" minlength="3" value="{{city}}"/>
  {{/profile}}
  {{#if signed}}
  <label for="signature">Signature</label>
  <img src="{{signature}}" height="105px" name="signature">
  {{/if}}
<button type="text" class="submit">Change</button>
</div>
</form>


<div id="alert"><p >{{#if success}}
Successfully Changed
{{/if}}</p></div>


{{#if signed}}
<div >
<form style="display:none" action="/deleteUser" method="post">
<input name="delete" style="display:none" value="delete">
<button type="submit" id="delete"></button>
</form>
<label  id="delete" class="input" style="text-decoration: underline" for="delete">Delete</label>
</div>
{{/if}}
<script>
  let submitButton=$(".submit");
  let alert = $("#alert");
  submitButton.click(function(e){
    var emptyTextBoxes = $('#emptyInput').filter(function() { return this.value == ""; });
    if(emptyTextBoxes.length > 0){
      alert.text("Please do not leave empty fields")
      e.preventDefault();
    };
  })
</script>